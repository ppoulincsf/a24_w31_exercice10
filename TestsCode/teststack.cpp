#include "stdafx.h"
#include "CppUnitTest.h"
#include "Stack.h"
using namespace Microsoft::VisualStudio::CppUnitTestFramework;

namespace TestsCode
{
  TEST_CLASS(StackTests)
  {
  public:

    TEST_METHOD(Contructor)
    {
    /*
      // TODO: Ici, votre code de test
      Stack stack;

      Assert::IsTrue(stack.isEmpty());
      Assert::AreEqual((unsigned int)0, stack.size());
    
    */
    }
    //Complétez les tests ici
    //Exemples de tests à produire:
    //Tester un push sur pile vide (vérifiez la taille, si la pile est vide, le contenu du top)
    //Tester un push sur une pile non vide (vérifiez la taille, si la pile est vide, le contenu du top)
    //Tester un pop sur une pile non vide (vérifiez la taille, si la pile est vide, le contenu du top)
    
    //Bonus (car syntaxe différente avec les exceptions)
    //Tester un pop d'une pile vide (exception)
    //Tester un top d'une pile vide (exception)
    
    //Vous devez implémenter tous les tests visant à mettre à l'épreuve votre structure
  };
}